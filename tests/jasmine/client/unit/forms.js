Template.stub('register');

describe("forms", function() {
  describe("Test form validating method", function() {
    it("return that form in data is not valid when all fields are empty", function() {
      var user = {
        email: '',
        userName: '',
        profile: {
          userName: '',
          firstName: '',
          lastName: ''
        }
      };

      var temp =

      expect($('a.btn').length).toEqual(2);
    });
  });
});
